[package]
name = "imu-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true, default-features = false }
